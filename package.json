{
  "name": "example-pulumi-lambda-api",
  "version": "1.0.0",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@pulumi/aws": "^6.66.0",
    "@pulumi/pulumi": "^3.142.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
